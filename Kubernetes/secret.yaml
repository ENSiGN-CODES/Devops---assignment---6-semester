# NOTE: This file is a TEMPLATE only.
# Real secrets are stored in AWS Secrets Manager and injected via
# the AWS Secrets Store CSI Driver - never committed to Git.
#
# To create the secret manually for local dev:
#   kubectl create secret generic db-secret \
#     --from-literal=host=<RDS_ENDPOINT> \
#     --from-literal=password=<DB_PASSWORD>

apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
# data values below are base64-encoded PLACEHOLDERS only
data:
  host: bG9jYWxob3N0    # "localhost" - replace with real value
  password: cG9zdGdyZXM= # "postgres" - replace with real value
